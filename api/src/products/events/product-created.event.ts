/** Emitted after a new product is committed, so every open window can show it live. */
export const PRODUCT_CREATED_EVENT = 'product.created';

// The payload is the product exactly as the API returns it (ProductResponse).
